---
description: 后端 API 目录、事务、缓存与并发规范
globs: src/api/**/*.ts
alwaysApply: false
---

# API 开发规范

## 目录

- `src/api/admin`：后台管理 API
- C 端或其他应用：在 `src/api/xxx` 新建模块，勿混入 `admin`

## 变更影响

- 修改 API 前评估对 admin-web、C 端、其他模块的影响
- 后台 API 变更若影响 C 端，需同步更新 C 端接口与适配层

## 事务

支付等关键写操作必须使用数据库事务。

## 并发与超卖

抢单、秒杀等场景使用 ioredis 分布式锁，防止多实例竞争与超卖。

## C 端缓存

- 调用频繁的 C 端读接口加 Redis 缓存
- 后台修改相关数据时，提供清除缓存按钮或接口
