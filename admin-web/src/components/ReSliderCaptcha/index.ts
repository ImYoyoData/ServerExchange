import { withInstall } from "@pureadmin/utils";
import reSliderCaptcha from "./src/index.vue";

/** 滑块验证码组件（基于 captcha-pro） */
export const ReSliderCaptcha = withInstall(reSliderCaptcha);

export default ReSliderCaptcha;
